{
  "_note": "Do not store real credentials here. Use CYPRESS_TEST_USER_EMAIL and CYPRESS_TEST_USER_PASSWORD env vars instead.",
  "disabled_user": {
    "email": "set_via_env",
    "password": "set_via_env"
  }
}
